import React from "react";

const AuthLayout = ({ children }) => {
  return <div className="bg-gradient-to-br from-indigo-700 via-purple-600 to-pink-500 h-screen flex items-center justify-center">{children}</div>;
};

export default AuthLayout;
